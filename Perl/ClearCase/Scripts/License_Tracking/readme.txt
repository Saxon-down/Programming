This script currently uses the following to work:

1. It's installed on e:\scripts\license_tracking2 on VL2RAT002 (\\vl2rat002\lt2$)
2. There's a scheduled job to run the BATCH file lt.bat
3. The script needs the following files (it can create everything else as required):
	lt.bat
	license_tracking.pl
	\images